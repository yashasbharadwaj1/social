 py -m venv myenv     
 myenv\scripts\activate 
 pip install django
 django-admin startproject myproject
 settings.py --- add database connection  
 cd myproject 
 django-admin startapp account 
 add account in settings 
 pip install mysqlclient      
 py manage.py runserver 
 git init from root 
 create a new repo from website follow full list of commands 
  add this to templates list in settings.py 'DIRS': [os.path.join(BASE_DIR,'templates')],        
create templates folders
 static folder
 STATIC_URL = 'static/'
STATIC_ROOT=os.path.join(BASE_DIR, 'staticfiles')
STATICFILES_DIRS=(os.path.join(BASE_DIR, 'static'),)
create models handle profile 
FOR PROFILE IMAGES WHICH COMES UNDER THE CATEGORY OF MEDIA 
configure that and add these to settings.py  
MEDIA_URL='/media/'
MEDIA_ROOT=os.path.join(BASE_DIR, 'media')
then go ahead and configure urls where in u import 
from django.conf.urls.static import static 
urlpatterns=urlpatterns + static(settings.MEDIA_URL,document_root=settings.MEDIA_ROOT)
  add this is to models.py
  profileimg=models.ImageField(upload_to='profile_images',default='blank-profile-picture.png') 
this is the way u configure the User model
from django.contrib.auth import get_user_model
# Create your models here.
User=get_user_model()
class Profile(models.Model):
    user=models.ForeignKey(User,on_delete=models.CASCADE)
add urls and views for signup and login 
whenever making post request add csrf token 